# THREAT_MODEL — hw-hub-frontend

Housework Hub フロントエンド（Vue 3 / TypeScript SPA）の脅威モデル。
LLM ベースのセキュリティスキャン（`claude-code-security-review` / defending-code-reference-harness）および
人手のレビューが、**何を守り・何を信頼し・何を対象外とするか**を共有するための土台ドキュメント。

> 本書は Anthropic「Using LLMs to secure source code」の Find-and-Fix ループ Step 1（Threat Modeling）に対応する。
> スキャン結果のトリアージ時は、必ず本書の「信頼する入力」「スコープ外」を参照し、重大度の過大評価を避けること。

- 対象リポジトリ: `hw-hub-frontend`
- 種別: ブラウザ上で動作する SPA（公開配信、認証ありの SaaS フロントエンド）
- 最終更新: 2026-06-19
- ステータス: ドラフト（Phase 0。実装変更ではなくレビュー用の合意文書）

---

## 1. システムコンテキスト（What are we working on?）

家事・買い物・世帯管理を行う家族向け SaaS「Housework Hub」のフロントエンド。

| 項目         | 内容                                                                       |
| ------------ | -------------------------------------------------------------------------- |
| 実行環境     | エンドユーザーのブラウザ（信頼できないクライアント）                       |
| 配信         | 静的アセットを CloudFront / S3 から配信                                    |
| バックエンド | `VITE_API_BASE_URL`（本番: `https://api.familyapp-hwhub.com`）への REST    |
| 認証         | JWT アクセストークン（Bearer）。`axios` インターセプタで付与               |
| 認可         | サーバー側で強制。フロントの `requiresAuth` / `requiresAdmin` は UX ガード |
| 外部連携     | Google OAuth ログイン / S3 presigned URL への直接アップロード              |
| 状態管理     | Pinia ストア、一部を `localStorage` に永続化                               |

### コンポーネント概要

- `src/api/*` … REST クライアント（`client.ts` が共通 axios インスタンスとインターセプタ）
- `src/stores/*` … Pinia ストア（`authStore.ts` がセッションの中核）
- `src/router/index.ts` … ルーティングと `beforeEach` ナビゲーションガード
- `src/domain/*` … ドメインモデル / 型
- `src/views/*`, `src/components/*` … 画面・UI（`admin/` 配下は管理者機能）

---

## 2. 守るべき資産（Assets）

| 資産                    | 説明                                                     | 影響度 |
| ----------------------- | -------------------------------------------------------- | ------ |
| JWT アクセストークン    | `localStorage['hwhub.auth']` に保存。漏洩=なりすまし     | 高     |
| ユーザー個人情報        | email / displayName / 世帯・家事・買い物データ           | 高     |
| 世帯（household）データ | 世帯メンバー・招待・権限。他世帯への越境参照は重大       | 高     |
| 管理者機能              | お知らせ・テンプレート・ロール・問い合わせ・ユーザー管理 | 高     |
| OAuth 連携の整合性      | Google ログイン / 連携フローの token 受け渡し            | 中     |
| アップロード資産        | ユーザーアイコン・買い物添付（presigned URL 経由で S3）  | 中     |

---

## 3. エントリポイント / 攻撃面（Entry points）

| #   | エントリポイント               | 信頼できない入力                                 |
| --- | ------------------------------ | ------------------------------------------------ |
| E1  | ログイン / 登録フォーム        | email・password・displayName・招待トークン       |
| E2  | OAuth コールバック             | URL 経由で戻る accessToken                       |
| E3  | API レスポンス                 | バックエンドから受け取る JSON 全般               |
| E4  | URL / ルートパラメータ・クエリ | `redirect` クエリ、各種 ID パラメータ            |
| E5  | `localStorage`                 | `hwhub.auth` / `hwhub_theme` / `hwhub_locale` 等 |
| E6  | ファイルアップロード           | アイコン・添付ファイル（name / mimeType）        |
| E7  | npm 依存パッケージ             | サプライチェーン（`package-lock.json`）          |

---

## 4. 信頼境界（Trust boundaries）

```
[ユーザー/ブラウザ:信頼できない] ──TLS──> [Backend API:信頼境界の中心] ──> [DB / S3]
        │                                        ▲
        ├─ localStorage（クライアント保管・改ざん可能）
        ├─ ルートガード（UX のみ。認可の砦ではない）
        └─ OAuth リダイレクト ──> [Google] ──> Backend ──> token 返却
```

**最重要原則: フロントエンドのコードとクライアント保管データは一切信頼できない。**
ルートガード・画面の出し分け・クライアント側バリデーションはすべて UX 上の利便性であり、
**認可・入力検証の最終的な強制は必ずバックエンド側で行われる前提**で評価する。

---

## 5. 想定する脅威（What can go wrong?）

実装確認に基づく、フロントエンド固有の重点項目。

| ID  | 脅威                                                                                                          | 関連                        | 重大度 |
| --- | ------------------------------------------------------------------------------------------------------------- | --------------------------- | ------ |
| T1  | **XSS によるトークン窃取**: アクセストークンが `localStorage` にあるため、XSS 1 件で奪取される                | `authStore.ts` / 全描画箇所 | 高     |
| T2  | **CSP 不在**: `index.html` に Content-Security-Policy が無く、インライン script を許容                        | `index.html`                | 中〜高 |
| T3  | **OAuth トークンの受け渡し**: `completeOAuthLogin` に渡る token の出所（URL fragment 等）と履歴・Referer 残留 | `authStore.ts`              | 中     |
| T4  | **オープンリダイレクト**: ログイン後復帰先 `redirect` クエリの検証不足で外部 URL に誘導される懸念             | `client.ts` / `router`      | 中     |
| T5  | **クライアント認可の誤用**: `requiresAdmin` 等を「守り」と誤認し、サーバー側強制が抜ける                      | `router/index.ts`           | 中     |
| T6  | **API レスポンスの過信**: 他ユーザー/他世帯データや想定外フィールドをそのまま描画・保存                       | `src/api/*` / stores        | 中     |
| T7  | **アップロードの取り扱い**: mimeType/拡張子をクライアント値のみで判断、presigned URL の悪用                   | `userApi` / 添付系          | 中     |
| T8  | **依存パッケージの既知脆弱性**: axios 等の transitive を含むサプライチェーン                                  | `package-lock.json`         | 中     |
| T9  | **機微情報の混入**: ソース・ビルド成果物・`.env*` への秘密値混入やログ出力                                    | `.env.*` / build            | 中     |

---

## 6. 現状の対策と対応方針（What are we going to do about it?）

| 脅威 | 現状の対策                                                  | 追加で検討すること                                              |
| ---- | ----------------------------------------------------------- | --------------------------------------------------------------- |
| T1   | アプリコードに `v-html` / `innerHTML` / `eval` は不使用     | Vue の自動エスケープ維持。トークン保管方式（Cookie 化等）の検討 |
| T2   | —                                                           | CSP の導入（CloudFront ヘッダ or meta）、インライン script 排除 |
| T3   | `isBootstrapping` / `isAuthTransition` で遷移中の暴発を抑止 | token は fragment 受け渡し＋即時履歴置換、Referrer-Policy       |
| T4   | `redirect` は基本 `fullPath`（内部パス）を利用              | 復帰先は内部パスのみ許可（外部スキーム/ホストを拒否）           |
| T5   | `beforeEach` で `requiresAuth` / 権限を確認                 | 「UX ガードでありセキュリティ境界ではない」前提を維持           |
| T6   | 共通 `ErrorResponse` ハンドリングを集約                     | 描画前の型/権限前提の確認、最小権限でのデータ取得               |
| T7   | presigned URL でアップロードを直接化（API 経由しない）      | サーバー側での content-type / サイズ / 所有者検証に依存         |
| T8   | `package-lock.json` で固定                                  | `npm audit` / SCA を CI に（Phase 1）                           |
| T9   | `.gitignore` で `.env` ローカル分を除外                     | ビルド成果物・ログへの秘密混入チェック                          |

---

## 7. 信頼する入力（Trusted inputs）— スキャン時の過大評価を防ぐ

以下は**このフロントの脅威モデル上は信頼する前提**とし、ここを根拠にした指摘は重大度を上げない。

- **バックエンド API のレスポンス整合性・認可結果**: 認可・所有権・入力検証はサーバーで強制される前提。
  フロントは「サーバーが拒否すべきものは拒否する」ことを信頼する（ただし描画時の XSS 耐性は別途必要）。
- **TLS / CDN**: 通信は HTTPS、配信は CloudFront 前提。中間者は対象外。
- **ビルドツールチェーン**: Vite / vue-tsc / 公式プラグインは信頼する。
- **ブラウザのサンドボックス・同一オリジンポリシー**: ブラウザ実装は信頼する。

---

## 8. スコープ外（Out of scope）

このリポジトリの脅威モデルでは**扱わない**（別リポジトリ/別レイヤの責務）。

- サーバー側の認証・認可・入力検証・SQL/コマンドインジェクション → `hw-hub-backend`
- インフラ・WAF・ネットワーク・S3 バケットポリシー・IAM → `hw-hub-infra`
- DB スキーマ・データ保護 → `hw-hub-database`
- モバイルアプリ固有の脅威 → `hw-hub-mobile`
- 物理セキュリティ、ソーシャルエンジニアリング、エンドユーザー端末のマルウェア感染

> 補償的統制（WAF・認証ゲートウェイ・サーバー側レート制限など）はソースに現れないため、
> フロント単体のスキャンでは検証できない。それらを根拠に「未対策」と断ずる指摘は誤検知として扱う。

---

## 9. レビュー観点チェック（Did we do a good job?）

スキャン/レビューで重点的に確認する問い:

1. ユーザー由来・API 由来のデータが、エスケープされずに DOM/属性/URL に流れていないか
2. アクセストークンや個人情報がログ・URL・サードパーティへ漏れていないか
3. リダイレクト・遷移先に外部 URL を許す経路がないか
4. クライアント側ガードを唯一の防御として扱っている箇所はないか（＝サーバー強制の前提が崩れていないか）
5. 依存パッケージに既知の高/重大脆弱性がないか
6. `.env*` / 設定 / ビルド成果物に秘密値が混入していないか

---

## 10. 更新運用

- 認証・通信・アップロード・外部連携など**信頼境界に関わる変更時**に本書を更新する。
- スキャン結果のトリアージで「信頼する入力 / スコープ外」の判断に使った前提が誤っていた場合、本書を正とせず修正する（モデルは本書を真実として参照するため）。
- Retro でセキュリティ指摘を棚卸しする際、本書の見直し要否を確認する。
